const checkoutButtons=document.querySelectorAll('[data-checkout]');checkoutButtons.forEach(button=>{button.addEventListener('click',()=>{if(typeof gtag==='function'){gtag('event','click_checkout',{event_category:'cta',event_label:button.dataset.label||'checkout'});}});});const legacySource='https://raw.githubusercontent.com/OficinaEficente/Site_Gol/main/index.html';const imageRanges={2:{start:5072857,end:13383332,mime:'image/png'}};const imageCache=new Map();async function loadLegacyImage(index){if(imageCache.has(index))return imageCache.get(index);const range=imageRanges[index];if(!range)return null;const promise=fetch(legacySource,{headers:{Range:`bytes=${range.start}-${range.end}`}}).then(async response=>{if(!response.ok||response.status!==206)return null;const base64=(await response.text()).trim();return `data:${range.mime};base64,${base64}`;}).catch(()=>null);imageCache.set(index,promise);return promise;}const lazyPhotos=[...document.querySelectorAll('[data-legacy-img]')];const loadPhoto=async img=>{if(img.dataset.loaded)return;img.dataset.loaded='1';const src=await loadLegacyImage(Number(img.dataset.legacyImg));if(src)img.src=src;};if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){loadPhoto(entry.target);observer.unobserve(entry.target);}});},{rootMargin:'350px'});lazyPhotos.forEach(img=>observer.observe(img));}else{lazyPhotos.forEach(loadPhoto);}